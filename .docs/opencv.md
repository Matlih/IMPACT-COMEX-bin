# Download

```sh
sudo apt-get install python3-numpy python3-opencv python3-torch python3-torchvision build-essential cmake libgtk-3-dev libboost-all-dev plank compton

python3 -m venv --system-site-packages .venv
source .venv/bin/activate
python -m pip install --upgrade pip setuptools wheel
python -m pip install --no-cache-dir --no-deps ultralytics
```